{
  "name": "odbc-example-worker",
  "version": "1.0.0",
  "description": "A sample Node.js app that processes requests",
  "license": "Apache-2.0",
  "scripts": {
    "pretest": "eslint --ignore-path ../.gitignore .",
    "test": "node -e \"console.log('no tests found.')\"",
    "start": "node server.js",
    "dev": "SERVICE_BINDING_ROOT=bindings nodemon app.js"
  },
  "dependencies": {
    "kube-service-bindings": "https://github.com/nodeshift/kube-service-bindings",
    "pg": "^8.7.3"
  }
}
